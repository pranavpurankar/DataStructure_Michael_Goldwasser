#!/usr/bin/env python3

'''
Reinforcement 1.2
Write a short Python function, is_even(k), that takes an integer value and
returns True if k is even, and False otherwise. However, your function cannot
use the multiplication, modulo, or division operator.
'''


def is_even(k):
    if k in for eve in range(0, 10, 2):
        return True
    else:
        return False


print(is_even(6))
